---
- name: Debug update_secrets
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    # Define your connection variables here (ideally injected via Ansible Vault or AAP Credentials)
    gateway_host: "https://vm-10-1-230-59.ans.cee.ral3.lab.eng.rdu2.redhat.com"
    gateway_user: "admin"
    gateway_pass: "gateway"
  tasks:
    - name: "test"
      ansible.platform.user:
        username: "test"
        first_name: "test"
        last_name: "test"
        password: "test12345678"
        update_secrets: true
        # Explicit connection parameters injected into the module
        aap_hostname: "{{ gateway_host }}"
        aap_username: "{{ gateway_user }}"
        aap_password: "{{ gateway_pass }}"
        aap_validate_certs: false # Set to true if you are using a valid enterprise CA
